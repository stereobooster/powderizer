{
  "name": "instaparsejs-root",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "pnpm@9.2.0",
  "dependencies": {
    "turbo": "^2.0.9",
    "vitest": "^1.5.2",
    "typescript": "^5.5.2"
  },
  "scripts": {
    "test": "vitest",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "clean": "turbo run clean"
  }
}
